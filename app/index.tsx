import { Alert, Pressable, StyleSheet, Text, View } from "react-native";

export default function Home() {
    return (
        <View className="bg-amber-600 flex items-center justify-center h-full">
            <Text>Select uploadthingImage</Text>
        </View>
    );
}
